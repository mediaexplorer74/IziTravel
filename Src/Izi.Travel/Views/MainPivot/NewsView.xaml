<Page
    x:Class="IziTravel.Views.MainPivot.NewsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:IziTravel.Views.MainPivot"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    xmlns:micro="local:Caliburn.Micro;assembly=Caliburn.Micro"
    xmlns:behaviors="local:IziTravel.Interaction.Behaviors"
    Foreground="{StaticResource AppBarItemForegroundThemeBrush}"
    d:DesignHeight="480" d:DesignWidth="480"
    >

    <Grid >
        
            <ListBox ItemsSource="{Binding NewsItems}"
                                            Margin="24 0 0 0" 
                                            Style="{StaticResource ListBoxStyle}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    
                    
                        <StackPanel VerticalAlignment="Top"
                                     Margin="0 0 24 18"
                                     >
                        <TextBlock TextDecorations="Underline" 
                                   FontSize="24" 
                                   TextWrapping="Wrap" 
                                   HorizontalAlignment="Left" 
                                   Foreground="{StaticResource PhoneAccentBrush}" 
                                   Text="{Binding Title}" />
                        <TextBlock TextWrapping="Wrap" 
                                   Text="{Binding Body}" />
                        <TextBlock Foreground="{StaticResource PhoneSubtleBrush}" 
                                   Text="{Binding Timestamp}" />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
            
        </ListBox>
    </Grid>
</Page>
    
    
