<UserControl x:Class="Izi.Travel.Views.Profile.History.ProfileHistoryListView"
     xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
     xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="using:Izi.Travel.Controls"
             xmlns:i="using:Microsoft.Xaml.Interactivity"
             xmlns:behaviors="clr-namespace:Izi.Travel.Core.Components.Behaviors"
             xmlns:converters="clr-namespace:Izi.Travel.Mtg.Converters"
             xmlns:components="clr-namespace:Izi.Travel.Common.Components"
             xmlns:commoncontrols="clr-namespace:Izi.Travel.Common.Controls"
             xmlns:history="clr-namespace:Izi.Travel.ViewModels.Profile.History"
             xmlns:placeholders="using:Izi.Travel.Controls.Placeholders"
             mc:Ignorable="d"
             d:DesignHeight="600"
             d:DesignWidth="400">



    <UserControl.Resources>


        <DataTemplate x:Key="HistoryListItemTemplate">
            <Grid 
                  Background="Transparent"
                  Height="120"
                  VerticalAlignment="Top" 
                  d:DataContext="{d:DesignInstance history:ProfileHistoryListItemViewModel}"
                  Margin="0,0,25,25">

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>



                <TextBlock Grid.Row="0" Grid.Column="1"
                           Text="{Binding Title}" 
                           FlowDirection="{Binding Language, Converter={StaticResource LanguageToFlowDirectionConverter}}"
                           Foreground="{StaticResource IziTravelDarkBrush}" 
                           FontSize="{StaticResource IziTravelFontSizeNormal}"
                            
                           TextWrapping="Wrap"
                           TextTrimming="WordEllipsis" 
                           MaxHeight="62" />
                <TextBlock Grid.Row="1" Grid.Column="1"
                           Foreground="{StaticResource IziTravelDarkGrayBrush}" 
                           FontSize="{StaticResource IziTravelFontSizeSmall}"
                           
                           Text="{Binding Type, Converter={StaticResource MtgObjectTypeToStringConverter}}"
                           TextTrimming="WordEllipsis"
                           Margin="0,5,0,0" />
                <TextBlock Grid.Row="2" Grid.Column="1"
                           Foreground="{StaticResource IziTravelBlueBrush}" 
                           FontSize="{StaticResource IziTravelFontSizeSmall}"
                           
                           Text="{Binding StateDownloadedString}"
                           VerticalAlignment="Top"
                           HorizontalAlignment="Right"
                           Margin="0,5,0,0" />
            </Grid>
        </DataTemplate>

        <DataTemplate x:Key="HistoryGroupHeaderTemplate">
            <TextBlock Text="{Binding Title, Converter={StaticResource StringToUpperConverter}}"
                       
                       FontSize="{StaticResource IziTravelFontSizeNormal}"
                       Margin="0,0,24,24" 
                       Opacity="0.5"
                       d:DataContext="{d:DesignInstance history:ProfileHistoryListGroupItemViewModel}" />
        </DataTemplate>
    </UserControl.Resources>

    <Grid>
        <ListBox ItemsSource="{Binding Items}"
                 ItemTemplate="{StaticResource HistoryListItemTemplate}"
                 Visibility="{Binding IsListEmpty, Converter={StaticResource BoolToInvisibilityConverter}}"
                 Style="{StaticResource IziTravelListBoxStyle}">
        </ListBox>

            <StackPanel Margin="-25,70,0,0"
                        Visibility="{Binding IsListEmpty, Converter={StaticResource BoolToVisibilityConverter}}">
                <Image Source="/Assets/Images/image.empty.history.png" 
                       Stretch="None" />
                <TextBlock Margin="25,50,25,0"
                           Foreground="{StaticResource IziTravelDarkBrush}"
                           FontSize="{StaticResource IziTravelFontSizeNormal}"
                           TextWrapping="Wrap"
                           TextAlignment="Center"
                           Text="{Binding LocalizedResources.MessageListHistoryIsEmpty, Source={StaticResource LocalizedStrings}}" />
            </StackPanel>
        </Grid>
</UserControl>