<UserControl x:Class="Izi.Travel.Views.Featured.FeaturedListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="using:Izi.Travel.Core.Controls"
             xmlns:viewmodels="using:Izi.Travel.ViewModels.Featured"
             xmlns:commoncontrols="using:Izi.Travel.Common.Controls"
             xmlns:placeholders="using:Izi.Travel.Controls.Placeholders"
             xmlns:converters="using:Izi.Travel.Core.Converters"
             xmlns:i="using:Microsoft.Xaml.Interactivity"
             xmlns:behaviors="clr-namespace:Izi.Travel.Core.Components.Behaviors"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance viewmodels:FeaturedListViewModel}"
             d:DesignHeight="800"
             d:DesignWidth="400">    
    
    
    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        
        <DataTemplate x:Key="FeaturedListHeaderTemplate">
            <TextBlock Text="{Binding LocalizedResources.FlyoutFeaturedTitle, Source={StaticResource LocalizedStrings}}"
                       Style="{StaticResource FlyoutTitleStyle}"                       
                       Margin="25,30,25,25"
                       Visibility="{Binding IsHeaderVisible, Converter={StaticResource BoolToVisibilityConverter}}" />
        </DataTemplate>
        
        <DataTemplate x:Key="FeaturedListItemTemplate">
            <Grid 
                  Height="435"
                  Margin="25,0,25,25"
                  d:DataContext="{d:DesignInstance viewmodels:FeaturedListItemViewModel}">
                
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                
                <commoncontrols:ImagePlaceholder Grid.Row="0" Grid.RowSpan="3"
                                                 ImageSource="{Binding ImageUrl}"
                                                 ImageStretch="UniformToFill" />

                
                <Border Grid.Row="0" Grid.RowSpan="3">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0.5" EndPoint="0,1">
                            <GradientStop Offset="0" Color="#20000000" />
                            <GradientStop Offset="1" Color="#FF000000" />
                        </LinearGradientBrush>
                    </Border.Background>
                </Border>

                
                <TextBlock Grid.Row="1" 
                           Text="{Binding SubTitle}" 
                           Foreground="{StaticResource IziTravelLightBrush}" 
                            
                           FontSize="18" 
                           HorizontalAlignment="Left"
                           TextTrimming="WordEllipsis"
                           Margin="24,0,0,0" />

                
                <TextBlock Grid.Row="2" 
                           Text="{Binding Title}" 
                           Foreground="{StaticResource IziTravelLightBrush}" 
                            
                           FontSize="26" 
                           TextWrapping="Wrap"
                           TextTrimming="WordEllipsis"
                           HorizontalAlignment="Left"
                           Margin="24,15,24,24" />
            </Grid>
        </DataTemplate>

        <DataTemplate x:Key="FeaturedListFooterTemplate">
            <Grid Background="{StaticResource IziTravelVioletBrush}" Height="500" Margin="0,25,0,-350"
                  Visibility="{Binding IsFooterVisible, Converter={StaticResource BoolToVisibilityConverter}}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                                              
                
                <Button Grid.Row="0"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Bottom"
                        Margin="0,50,25,25"
                        Command="{Binding ExploreCommand}">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Grid Background="Transparent">
                                <VisualStateManager.VisualStateGroups>
                                    <VisualStateGroup x:Name="CommonStates">
                                        <VisualState x:Name="Normal"/>
                                        <VisualState x:Name="MouseOver" />
                                        <VisualState x:Name="Pressed">
                                            <Storyboard>
                                                <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Fill"
                                                                               Storyboard.TargetName="PartEllipse">
                                                    <DiscreteObjectKeyFrame KeyTime="0" 
                                                                            Value="{StaticResource IziTravelLightBlueBrush}" />
                                                </ObjectAnimationUsingKeyFrames>
                                            </Storyboard>
                                        </VisualState>
                                    </VisualStateGroup>
                                </VisualStateManager.VisualStateGroups>

                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <TextBlock x:Name="PartTitle"
                                           Grid.Column="0" 
                                           Foreground="{StaticResource IziTravelLightBrush}"
                                           
                                           FontSize="{StaticResource IziTravelFontSizeExtraMedium}"
                                           VerticalAlignment="Center"
                                           HorizontalAlignment="Center"
                                           Text="{Binding LocalizedResources.MessageFeaturedActionExplore, Source={StaticResource LocalizedStrings}}" 
                                           Margin="0,0,25,0"/>

                                <Ellipse x:Name="PartEllipse"
                                         Grid.Column="1"
                                         VerticalAlignment="Center" 
                                         HorizontalAlignment="Center" 
                                         Stroke="{StaticResource IziTravelLightBrush}"
                                         StrokeThickness="4"
                                         Width="50" Height="50" />

                                <Image Source="/Assets/Icons/appbar.next.png"
                                       Grid.Column="1"
                                       VerticalAlignment="Center"
                                       HorizontalAlignment="Center"
                                       Width="50" Height="50" />
                            </Grid>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>
        </DataTemplate>
    </UserControl.Resources>
    
    
    <placeholders:ProgressOverlay IsBusy="{Binding IsDataLoading}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Header -->
            <ContentControl Grid.Row="0" 
                          ContentTemplate="{StaticResource FeaturedListHeaderTemplate}" 
                          HorizontalContentAlignment="Stretch"
                          Visibility="{Binding IsHeaderVisible, Converter={StaticResource BoolToVisibilityConverter}}" />

            <!-- Content - Using ListView with ListViewLoadBehavior -->
            <Grid Grid.Row="1">
                <ListView x:Name="FeaturedList"
                         ItemsSource="{Binding Items}"
                         ItemTemplate="{StaticResource FeaturedListItemTemplate}"
                         Loaded="OnLoaded"
                         SelectionChanged="OnSelectionChanged"
                         ScrollViewer.VerticalScrollBarVisibility="Auto"
                         ScrollViewer.VerticalScrollMode="Auto"
                         Padding="0,0,0,0"
                         IsItemClickEnabled="True"
                         SelectionMode="None"
                         ShowsScrollingPlaceholders="False"
                         ContainerContentChanging="OnContainerContentChanging"
                         Visibility="{Binding IsListEmpty, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=Inverse}">
                    <ListView.Header>
                        <ContentControl Content="{Binding}" 
                                      ContentTemplate="{StaticResource FeaturedListHeaderTemplate}" 
                                      Visibility="{Binding IsHeaderVisible, Converter={StaticResource BoolToVisibilityConverter}}" />
                    </ListView.Header>
                    <ListView.Footer>
                        <ContentControl Content="{Binding}" 
                                      ContentTemplate="{StaticResource FeaturedListFooterTemplate}" />
                    </ListView.Footer>

                </ListView>

                <!-- Empty State -->
                <StackPanel VerticalAlignment="Center"
                            HorizontalAlignment="Center"
                            Visibility="{Binding IsListEmpty, Converter={StaticResource BoolToVisibilityConverter}}">
                    <Image Source="/Assets/Images/image.empty.featured.png" 
                           Stretch="None" 
                           HorizontalAlignment="Center"
                           Margin="0,0,0,20"/>
                    <TextBlock Text="{Binding LocalizedResources.MessageFeaturedListEmpty, Source={StaticResource LocalizedStrings}}"
                               Foreground="{StaticResource IziTravelGrayBrush}"
                               
                               FontSize="{StaticResource IziTravelFontSizeNormal}"
                               TextWrapping="Wrap"
                               TextAlignment="Center"
                               MaxWidth="300"
                               HorizontalAlignment="Center"/>
                    <HyperlinkButton Margin="0,30,0,0"
                                     Content="{Binding LocalizedResources.MessageFeaturedListEmptyAction, Source={StaticResource LocalizedStrings}}"
                                     Command="{Binding ExploreCommand}"
                                     HorizontalContentAlignment="Center"
                                     Style="{StaticResource HyperlinkButtonWrapStyle}" />
                </StackPanel>
            </Grid>
        </Grid>
    </placeholders:ProgressOverlay>
</UserControl>