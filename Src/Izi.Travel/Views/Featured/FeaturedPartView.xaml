<Page x:Class="Izi.Travel.Shell.Views.Featured.FeaturedPartView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:featured="clr-namespace:Izi.Travel.Shell.Views.Featured"
      xmlns:i="using:Microsoft.Xaml.Interactivity"
      xmlns:components="clr-namespace:Izi.Travel.Shell.Common.Components"
      xmlns:controls="using:Izi.Travel.Shell.Controls"
      mc:Ignorable="d"                           
      Style="{StaticResource IziTravelPageStyle}">
    
    <i:Interaction.Behaviors>
        <components:ViewAnalyticsBehavior />
    </i:Interaction.Behaviors>

    <Grid>        
        <controls:FlipView x:Name="PartFlipView"
                          VerticalAlignment="Stretch"
                          HorizontalAlignment="Stretch"
                          SelectedIndex="{Binding SelectedIndex, Mode=TwoWay}">
            <controls:FlipView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <ContentControl Content="{Binding}" 
                                      HorizontalContentAlignment="Stretch"
                                      VerticalContentAlignment="Stretch" />
                    </Grid>
                </DataTemplate>
            </controls:FlipView.ItemTemplate>
        </controls:FlipView>
        
        <ContentControl x:Name="List"
                      HorizontalContentAlignment="Stretch" 
                      VerticalContentAlignment="Stretch"
                      Visibility="Collapsed" />
    </Grid>
    
    <Page.Resources>
        <DataTemplate x:Key="WelcomeTemplate">
            <featured:FeaturedWelcomeView Margin="-1,0,0,0"
                                        IsLoading="{Binding DataContext.List.IsDataLoading, ElementName=PartFlipView}" />
        </DataTemplate>
        
        <DataTemplate x:Key="ListTemplate">
            <ContentControl Content="{Binding ElementName=List}" 
                          HorizontalContentAlignment="Stretch"
                          VerticalContentAlignment="Stretch" />
        </DataTemplate>
    </Page.Resources>
</Page>