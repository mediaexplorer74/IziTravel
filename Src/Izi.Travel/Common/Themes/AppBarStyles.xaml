<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Default CommandBar Style (no custom template to avoid WinUI dependency) -->
    <Style x:Key="IziTravelCommandBarStyle" TargetType="CommandBar">
        <Setter Property="Background" Value="{ThemeResource SystemControlBackgroundAltHighBrush}" />
        <Setter Property="Foreground" Value="{ThemeResource SystemControlForegroundBaseHighBrush}" />
        <Setter Property="DefaultLabelPosition" Value="Right" />
        <Setter Property="IsOpen" Value="False" />
        <Setter Property="MinHeight" Value="48" />
    </Style>

    <!-- AppBarButton Style (lightweight) -->
    <Style x:Key="IziTravelAppBarButtonStyle" TargetType="AppBarButton">
        <Setter Property="Width" Value="48" />
        <Setter Property="Height" Value="48" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{ThemeResource SystemControlForegroundBaseHighBrush}" />
    </Style>

</ResourceDictionary>
