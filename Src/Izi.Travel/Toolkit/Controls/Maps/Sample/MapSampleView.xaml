<UserControl
    x:Class="Izi.Travel.Toolkit.Controls.Maps.Sample.MapSampleView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:maps="using:Izi.Travel.Toolkit.Controls.Maps"
    mc:Ignorable="d"
    d:DesignHeight="600"
    d:DesignWidth="800">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="12">
            <TextBlock Text="Map Sample" Style="{ThemeResource HeaderTextBlockStyle}" Margin="0,0,0,12"/>
            <TextBlock Text="{Binding StatusMessage}" Style="{ThemeResource BodyTextBlockStyle}" 
                       Foreground="{ThemeResource SystemControlHighlightAccentBrush}" TextWrapping="Wrap"/>
        </StackPanel>

        <!-- Map -->
        <Grid Grid.Row="1" Margin="12,0">
            <maps:MapControl ViewModel="{Binding Map}" 
                           ZoomLevel="{Binding ZoomLevel, Mode=TwoWay}"
                           Center="{Binding Center, Mode=TwoWay}"/>
        </Grid>

        <!-- Controls -->
        <CommandBar Grid.Row="2" DefaultLabelPosition="Right">
            <AppBarButton Icon="Zoom" Label="Zoom In" Command="{Binding ZoomInCommand}"/>
            <AppBarButton Icon="ZoomOut" Label="Zoom Out" Command="{Binding ZoomOutCommand}"/>
            <AppBarButton Icon="MapPin" Label="My Location" Command="{Binding CenterOnLocationCommand}"/>
        </CommandBar>
    </Grid>
</UserControl>
