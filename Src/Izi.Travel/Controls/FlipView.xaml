<UserControl
    x:Class="Izi.Travel.Controls.FlipView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:Izi.Travel.Core.Converters"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <converters:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter" />
        
        <Style x:Key="FlipViewItemStyle" TargetType="FlipViewItem">
            <Setter Property="Background" Value="{ThemeResource FlipViewItemBackground}" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="TabNavigation" Value="Local" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="FlipViewItem">
                        <Border x:Name="RootElement"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <Grid>
                                <ContentPresenter x:Name="ContentPresenter"
                                                ContentTemplate="{TemplateBinding ContentTemplate}"
                                                ContentTransitions="{TemplateBinding ContentTransitions}"
                                                Content="{TemplateBinding Content}"
                                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                                
                                <Button x:Name="ItemButton"
                                        Style="{StaticResource TransparentButtonStyle}"
                                        HorizontalAlignment="Stretch"
                                        VerticalAlignment="Stretch"
                                        Command="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=DataContext.ItemClickCommand}"
                                        CommandParameter="{Binding}"
                                        Visibility="{Binding IsItemClickEnabled, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource BoolToVisibilityConverter}}" />
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="IndexerItemStyle" TargetType="Rectangle">
            <Setter Property="Width" Value="8" />
            <Setter Property="Height" Value="8" />
            <Setter Property="Margin" Value="0,0,8,0" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Opacity" Value="0.65" />
        </Style>
    </UserControl.Resources>

    <Grid x:Name="RootGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Main FlipView -->
        <FlipView x:Name="FlipViewControl"
                  Grid.Row="0"
                  ItemsSource="{x:Bind ItemsSource, Mode=OneWay}"
                  SelectedIndex="{x:Bind SelectedIndex, Mode=TwoWay}"
                  ItemTemplate="{x:Bind ItemTemplate, Mode=OneWay}"
                  ItemTemplateSelector="{x:Bind ItemTemplateSelector, Mode=OneWay}"
                  ItemContainerStyle="{StaticResource FlipViewItemStyle}"
                  SelectionChanged="FlipViewControl_SelectionChanged"
                  Loaded="FlipViewControl_Loaded">
        </FlipView>

        <!-- Indexer Dots -->
        <ItemsControl x:Name="IndexerControl"
                     Grid.Row="1"
                     ItemsSource="{x:Bind ItemsSource}"
                     HorizontalAlignment="Center"
                     VerticalAlignment="Bottom"
                     Margin="0,0,0,15"
                     Visibility="{x:Bind ShowIndexer, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Rectangle Style="{StaticResource IndexerItemStyle}">
                        <Rectangle.Fill>
                            <SolidColorBrush Color="{Binding IsSelected, Converter={StaticResource BoolToColorConverter}, ConverterParameter=Selected}" />
                        </Rectangle.Fill>
                    </Rectangle>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
