<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Izi.Travel.Shell.Controls">
    
    <Style TargetType="local:LongListSelector">
        <Setter Property="Padding" Value="0"/>
        <Setter Property="TabNavigation" Value="Once"/>
        <Setter Property="IsTabStop" Value="True"/>
        <Setter Property="IsItemClickEnabled" Value="True"/>
        <Setter Property="SelectionMode" Value="Single"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:LongListSelector">
                    <Border BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Background="{TemplateBinding Background}">
                        <Grid>
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal"/>
                                    <VisualState x:Name="Disabled">
                                        <VisualState.Setters>
                                            <Setter Target="ScrollViewer.VerticalScrollBarVisibility" Value="Disabled"/>
                                        </VisualState.Setters>
                                    </VisualState>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                            
                            <ScrollViewer x:Name="ScrollViewer"
                                        TabNavigation="{TemplateBinding TabNavigation}"
                                        ZoomMode="Disabled"
                                        IsDeferredScrollingEnabled="False"
                                        BringIntoViewOnFocusChange="False">
                                <StackPanel>
                                    <ContentControl x:Name="HeaderContent"
                                                  Content="{TemplateBinding ListHeader}"
                                                  ContentTemplate="{TemplateBinding ListHeaderTemplate}"
                                                  Visibility="{Binding ListHeader, Converter={StaticResource NullToVisibilityConverter}}"/>
                                    
                                    <ItemsPresenter x:Name="ItemsPresenter"
                                                  Padding="{TemplateBinding Padding}"/>
                                    
                                    <ContentControl x:Name="FooterContent"
                                                  Content="{TemplateBinding ListFooter}"
                                                  ContentTemplate="{TemplateBinding ListFooterTemplate}"
                                                  Visibility="{Binding ListFooter, Converter={StaticResource NullToVisibilityConverter}}"/>
                                </StackPanel>
                            </ScrollViewer>
                            
                            <TextBlock x:Name="EmptyContent"
                                     Text="{TemplateBinding EmptyContent}"
                                     Visibility="Collapsed"
                                     HorizontalAlignment="Center"
                                     VerticalAlignment="Center"
                                     TextWrapping="Wrap"/>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
</ResourceDictionary>
