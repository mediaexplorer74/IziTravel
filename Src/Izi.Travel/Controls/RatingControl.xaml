<UserControl
    x:Class="Izi.Travel.Controls.RatingControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="40"
    d:DesignWidth="200">

    <UserControl.Resources>
        <Style x:Key="RatingStarStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{ThemeResource SystemControlForegroundBaseMediumBrush}" />
            <Setter Property="FontSize" Value="16" />
            <Setter Property="Margin" Value="2,0" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
    </UserControl.Resources>

    <StackPanel x:Name="RatingContainer" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
        <TextBlock x:Name="Star1" Style="{StaticResource RatingStarStyle}" Tapped="Star_Tapped" Tag="1" />
        <TextBlock x:Name="Star2" Style="{StaticResource RatingStarStyle}" Tapped="Star_Tapped" Tag="2" />
        <TextBlock x:Name="Star3" Style="{StaticResource RatingStarStyle}" Tapped="Star_Tapped" Tag="3" />
        <TextBlock x:Name="Star4" Style="{StaticResource RatingStarStyle}" Tapped="Star_Tapped" Tag="4" />
        <TextBlock x:Name="Star5" Style="{StaticResource RatingStarStyle}" Tapped="Star_Tapped" Tag="5" />
    </StackPanel>
</UserControl>
