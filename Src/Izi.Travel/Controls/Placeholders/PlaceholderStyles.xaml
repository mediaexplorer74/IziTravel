<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:placeholders="using:Izi.Travel.Controls.Placeholders"
    xmlns:commonControls="using:Izi.Travel.Common.Controls"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls">
    
    <x:Double x:Key="ItemMargin">4</x:Double>

    <!-- MarginSelector Style -->
    <Style TargetType="placeholders:MarginSelector">
        <Setter Property="Background" Value="LightGray"/>
        <Setter Property="MinHeight" Value="40"/>
    </Style>

    <!-- HeaderControl Style -->
    <Style TargetType="placeholders:HeaderControl">
        <Setter Property="Background" Value="LightBlue"/>
        <Setter Property="MinHeight" Value="48"/>
        <Setter Property="Padding" Value="12,8"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="placeholders:HeaderControl">
                    <Border Background="{TemplateBinding Background}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter Content="{TemplateBinding Content}"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ImagePlaceholder Style - Moved to Generic.xaml -->

    <!-- ProgressOverlay Style -->
    <Style TargetType="placeholders:ProgressOverlay">
        <Setter Property="Background" Value="#80000000"/>
        <Setter Property="IsHitTestVisible" Value="False"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="placeholders:ProgressOverlay">
                    <Grid x:Name="LayoutRoot">
                        <ContentPresenter x:Name="ContentPresenter"
                                      Content="{TemplateBinding Content}"
                                      ContentTemplate="{TemplateBinding ContentTemplate}"/>
                        <Border x:Name="Overlay"
                                Background="{TemplateBinding Background}"
                                Visibility="{Binding IsActive, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <ProgressRing IsActive="True"
                                         HorizontalAlignment="Center"
                                         VerticalAlignment="Center"/>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- LongListSelector Style (based on ListView) -->
    <Style TargetType="placeholders:LongListSelector" BasedOn="{StaticResource ListViewStyle}">
        <Setter Property="ItemContainerStyle" Value="{StaticResource ListViewItemStyle}"/>
    </Style>

    <!-- ItemsWrapGrid Style (replacement for WrapPanel) -->
    <Style x:Key="WrapGridStyle" TargetType="ItemsWrapGrid">
        <Setter Property="Orientation" Value="Horizontal"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="VerticalAlignment" Value="Top"/>
        <Setter Property="Margin" Value="0,0,0,8"/>
        <Setter Property="MaximumRowsOrColumns" Value="3"/>
        <Setter Property="CacheMode" Value="BitmapCache"/>
    </Style>

</ResourceDictionary>
