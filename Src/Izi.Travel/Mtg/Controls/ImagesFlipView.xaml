<UserControl x:Class="Izi.Travel.Mtg.Controls.ImagesFlipView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="using:Izi.Travel.Toolkit.Controls"
             xmlns:commoncontrols="using:Izi.Travel.Common.Controls"
             xmlns:converters="using:Izi.Travel.Core.Converters"
             mc:Ignorable="d"
             
             FontSize="{StaticResource PhoneFontSizeNormal}"
             Foreground="{StaticResource PhoneForegroundBrush}"
             d:DesignHeight="480" 
             d:DesignWidth="480">

    <UserControl.Resources>
        <converters:BoolToObjectConverter x:Key="FillConverter" 
                                          PositiveValue="{StaticResource IziTravelBlueBrush}"
                                          NegativeValue="{StaticResource IziTravelLightBrush}" />
    </UserControl.Resources>

    <Grid x:Name="LayoutRoot">
        <controls:FlipView x:Name="FlipView"
                           UpdateSelectionMode="BeforeTransition"
                           Background="{StaticResource IziTravelLightGrayBrush}"
                           SelectionChanged="FlipView_OnSelectionChanged">
            <controls:FlipView.ItemTemplate>
                <DataTemplate>
                    <commoncontrols:ImagePlaceholder Height="270"
                                                     ImageSource="{Binding PreviewUrl}"
                                                     ImageStretch="UniformToFill"
                                                     Tapped="Image_OnTapped" />
                </DataTemplate>
            </controls:FlipView.ItemTemplate>
        </controls:FlipView>
        <ItemsControl x:Name="ItemsControl"
                      Margin="0,0,0,15"
                      VerticalAlignment="Bottom"
                      HorizontalAlignment="Center">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Rectangle Opacity="0.65"
                               Width="8"
                               Height="8"
                               Margin="4,0,4,0"
                               VerticalAlignment="Center"
                               Fill="{Binding IsSelected, Converter={StaticResource FillConverter}}" />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>