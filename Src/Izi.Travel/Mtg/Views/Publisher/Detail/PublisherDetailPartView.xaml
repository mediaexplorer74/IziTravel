<Page x:Class="Izi.Travel.Shell.Mtg.Views.Publisher.Detail.PublisherDetailPartView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:controls="clr-namespace:Izi.Travel.Shell.Core.Controls"
      xmlns:micro="clr-namespace:Caliburn.Micro;assembly=Caliburn.Micro.Platform"
      xmlns:converters="using:Izi.Travel.Shell.Converters"
      mc:Ignorable="d"
      Style="{StaticResource IziTravelPageStyle}">

    <Page.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- UWP CommandBar -->
        <CommandBar Grid.Row="0"
                   Style="{StaticResource IziTravelCommandBarStyle}"
                   DefaultLabelPosition="Right"
                   IsOpen="False"
                   IsSticky="True"
                   Background="{ThemeResource SystemControlBackgroundAltHighBrush}">
            
            <!-- Commands will be populated from the ViewModel's AppBarButtons and AppBarMenuItems -->
            <CommandBar.PrimaryCommands>
                <AppBarButton x:Name="BackButton"
                             Icon="Back"
                             Label="Back"
                             Command="{Binding GoBackCommand}" />
            </CommandBar.PrimaryCommands>
            
            <!-- Secondary Commands (overflow menu) -->
            <CommandBar.SecondaryCommands>
                <!-- Menu items will be populated from ViewModel -->
            </CommandBar.SecondaryCommands>
        </CommandBar>

        <controls:ProgressOverlay Grid.Row="1" IsBusy="{Binding IsDataLoading}">
            <ContentControl Content="{Binding ActiveItem}"
                          HorizontalContentAlignment="Stretch"
                          VerticalContentAlignment="Stretch" />
        </controls:ProgressOverlay>
    </Grid>
</Page>