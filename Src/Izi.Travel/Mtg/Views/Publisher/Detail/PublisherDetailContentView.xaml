<UserControl x:Class="Izi.Travel.Mtg.Views.Publisher.Detail.PublisherDetailContentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             
             xmlns:i="using:Microsoft.Xaml.Interactivity"
             xmlns:behaviors="using:Izi.Travel.Core.Components.Behaviors"
             
             xmlns:detail="using:Izi.Travel.Mtg.ViewModels.Publisher.Detail"
             xmlns:controls="using:Izi.Travel.Common.Controls"
             xmlns:converters="using:Izi.Travel.Mtg.Converters"
             mc:Ignorable="d"
             Loaded="UserControl_Loaded">

    
    <UserControl.Resources>
        <converters:MtgObjectTypeToStringConverter x:Key="MtgObjectTypeToStringConverter" />
    </UserControl.Resources>

    
    <ListView ItemsSource="{Binding ActiveItem.Items}"
              Header="{Binding}"
              IsItemClickEnabled="True"
              ItemClick="ListView_ItemClick">
        <ListView.ItemContainerStyle>
            <Style TargetType="ListViewItem">
                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                <Setter Property="VerticalContentAlignment" Value="Stretch"/>
            </Style>
        </ListView.ItemContainerStyle>
        
        <ListView.HeaderTemplate>
            <DataTemplate>
                <Grid Margin="0,0,25,25" d:DataContext="{d:DesignInstance detail:PublisherDetailContentViewModel}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    
                    
                    <Grid Grid.Row="0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="270" />
                            <RowDefinition Height="25" />
                        </Grid.RowDefinitions>

                        
                        <Image Grid.Row="0" 
                           Source="{Binding BrandCoverUrl}"
                           Stretch="UniformToFill"
                           Height="270"
                           HorizontalAlignment="Stretch"
                           VerticalAlignment="Stretch"
                           Loaded="Image_Loaded"
                           ImageFailed="Image_Failed"/>

                        
                        <Border Grid.Row="0" Grid.RowSpan="2"
                                VerticalAlignment="Bottom"
                                HorizontalAlignment="Center"
                                Background="White"
                                BorderThickness="1"
                                BorderBrush="{StaticResource IziTravelLightGrayBrush}"
                                Height="120" Width="120">
                            <Image Source="{Binding BrandLogoUrl}"
                                   Stretch="UniformToFill"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   Loaded="Image_Loaded"
                                   ImageFailed="Image_Failed"/>
                        </Border>
                    </Grid>
                    
                    
                    <TextBlock Grid.Row="1" 
                               Text="{Binding Summary}"
                               
                               FontSize="{StaticResource IziTravelFontSizeNormal}"
                               TextWrapping="Wrap" 
                               Margin="0,25,0,0" 
                               Visibility="{Binding Summary, Converter={StaticResource NullToInvisibilityConverter}}"/>
                    
                    
                    <TextBlock Grid.Row="2"
                               Style="{StaticResource DetailInfoSectionHeaderStyle}"
                               TextWrapping="NoWrap"
                               HorizontalAlignment="Left"
                               Margin="0,25,0,0">
                        <Run Text="{Binding LocalizedResources.LabelAudioguides, Source={StaticResource LocalizedStrings}, Converter={StaticResource StringToUpperConverter}}" />
                        <Run Text="{Binding ContentCount}" />
                    </TextBlock>
                </Grid>
            </DataTemplate>
        </ListView.HeaderTemplate>
        <ListView.ItemTemplate>
            <DataTemplate>
                <Grid 
                      Background="Transparent"
                      Height="120"
                      VerticalAlignment="Top" 
                      Margin="0,0,25,25"
                      d:DataContext="{d:DesignInstance detail:PublisherDetailContentListItemViewModel}">

                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    
                    <Image Grid.Row="0" Grid.RowSpan="3" Grid.Column="0"
                           Source="{Binding ImageUrl}"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Stretch="UniformToFill"
                           Width="120"
                           Height="120"
                           Margin="0,0,25,0"
                           Loaded="Image_Loaded"
                           ImageFailed="Image_Failed"/>

                    
                    <TextBlock Grid.Row="0" Grid.Column="1"
                                   Text="{Binding Title}" 
                                   Foreground="{StaticResource IziTravelDarkBrush}" 
                                   FontSize="{StaticResource IziTravelFontSizeNormal}"
                                    
                                   TextWrapping="Wrap"
                                   TextTrimming="WordEllipsis" 
                                   MaxHeight="62" />
                    <TextBlock Grid.Row="1" Grid.Column="1"
                                   Foreground="{StaticResource IziTravelDarkGrayBrush}" 
                                   FontSize="{StaticResource IziTravelFontSizeSmall}"
                                   
                                   Text="{Binding Type, Converter={StaticResource MtgObjectTypeToStringConverter}}"
                                   TextTrimming="WordEllipsis"
                                   Margin="0,5,0,0" />
                </Grid>
            </DataTemplate>
        </ListView.ItemTemplate>
        <ListView.FooterTemplate>
            <DataTemplate>
                <Border Height="72" />
            </DataTemplate>
        </ListView.FooterTemplate>
    </ListView>
</UserControl>