<UserControl
    x:Class="Izi.Travel.Shell.Mtg.Views.Tour.TourListView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:i="using:Microsoft.Xaml.Interactivity"
    xmlns:corecontrols="using:Izi.Travel.Shell.Core.Controls"
    xmlns:commoncontrols="using:Izi.Travel.Shell.Common.Controls"
    xmlns:modulecontrols="using:Izi.Travel.Shell.Mtg.Controls"
    xmlns:Maps="using:Windows.UI.Xaml.Controls.Maps"
    xmlns:maps="using:Izi.Travel.Controls.Map"
    xmlns:viewmodels="using:Izi.Travel.Shell.Mtg.ViewModels.Tour.Map"
    xmlns:flyout="using:Izi.Travel.Shell.Core.Controls.Flyout"
    xmlns:coreconverters="using:Izi.Travel.Shell.Core.Converters"
    xmlns:moduleconverters="using:Izi.Travel.Shell.Mtg.Converters"
    xmlns:extensions="using:Izi.Travel.Shell.Core.Components.Extensions"
    xmlns:audio="using:Izi.Travel.Shell.Media.Views.Audio"
    xmlns:behaviors="using:Izi.Travel.Shell.Core.Components.Behaviors"
    xmlns:controls="using:Izi.Travel.Shell.Controls"
    mc:Ignorable="d"
    Style="{StaticResource IziTravelPageStyle}">
    
    <UserControl.Resources>
        
        <!-- Tilt effect storyboard for buttons -->
        <Storyboard x:Name="TiltStoryboard">
            <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.RenderTransform).(CompositeTransform.ScaleX)">
                <EasingDoubleKeyFrame KeyTime="0" Value="1"/>
                <EasingDoubleKeyFrame KeyTime="0:0:0.2" Value="0.95"/>
            </DoubleAnimationUsingKeyFrames>
            <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.RenderTransform).(CompositeTransform.ScaleY)">
                <EasingDoubleKeyFrame KeyTime="0" Value="1"/>
                <EasingDoubleKeyFrame KeyTime="0:0:0.2" Value="0.95"/>
            </DoubleAnimationUsingKeyFrames>
        </Storyboard>
        
        <moduleconverters:TourMapItemStateToBrushConverter x:Key="TourMapItemStateToForegroundConverter" 
                                                           NormalBrush="{StaticResource IziTravelBlueBrush}"
                                                           SelectedBrush="{StaticResource IziTravelLightBrush}" 
                                                           VisitedBrush="{StaticResource IziTravelGrayBrush}" />
        <moduleconverters:TourMapItemStateToBrushConverter x:Key="TourMapItemStateToBorderConverter" 
                                                           NormalBrush="{StaticResource IziTravelBlueBrush}"
                                                           SelectedBrush="{StaticResource IziTravelLightBrush}" 
                                                           VisitedBrush="{StaticResource IziTravelGrayBrush}" />
        <moduleconverters:TourMapItemStateToBrushConverter x:Key="TourMapItemStateToBackgroundConverter" 
                                                           NormalBrush="{StaticResource IziTravelLightBrush}"
                                                           SelectedBrush="{StaticResource IziTravelBlueBrush}">
            <moduleconverters:TourMapItemStateToBrushConverter.VisitedBrush>
                <SolidColorBrush Color="{StaticResource IziTravelLightColor}" Opacity="0.6" />
            </moduleconverters:TourMapItemStateToBrushConverter.VisitedBrush>
        </moduleconverters:TourMapItemStateToBrushConverter>

        <coreconverters:BoolToObjectConverter x:Key="MapPinIsSelectedToPlayContentMarginConverter"
                                              PositiveValue="0,8,0,0" NegativeValue="0,2,0,0" />
        <coreconverters:BoolToObjectConverter x:Key="MapPinIsSelectedToTextContentMarginConverter"
                                              PositiveValue="0,2,0,0" NegativeValue="0,0,0,2" />

        <Style TargetType="corecontrols:ActionRoundButton" x:Name="MapButton">
            <!-- Tilt effect replaced with UWP-compatible pointer animation -->
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5" />
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <CompositeTransform/>
                </Setter.Value>
            </Setter>
            <Setter Property="PointerPressed" Value="Button_PointerPressed"/>
            <Setter Property="PointerReleased" Value="Button_PointerReleased"/>
            <Setter Property="PointerCanceled" Value="Button_PointerReleased"/>
            <Setter Property="Foreground">
                <Setter.Value>
                    <SolidColorBrush Color="{StaticResource IziTravelDarkColor}" Opacity="0.4" />
                </Setter.Value>
            </Setter>
            <Setter Property="PressedBackground" Value="{StaticResource PhoneAccentBrush}" />
            <Setter Property="PressedForeground" Value="{StaticResource PhoneForegroundColor}" />
            <Setter Property="ImageSource" Value="/Assets/Icons/add.png" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Width" Value="48" />
            <Setter Property="Height" Value="30" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="corecontrols:ActionRoundButton">
                        <Grid Name="PartGrid">

                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal"/>
                                    <VisualState x:Name="MouseOver" />
                                    <VisualState x:Name="Pressed">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Background"
                                                                           Storyboard.TargetName="PartBorder">
                                                <DiscreteObjectKeyFrame KeyTime="0" 
                                                                        Value="{Binding PressedBackground, RelativeSource={RelativeSource TemplatedParent}}" />
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Fill"
                                                                           Storyboard.TargetName="PartEllipse">
                                                <DiscreteObjectKeyFrame KeyTime="0" 
                                                                        Value="{Binding PressedForeground, RelativeSource={RelativeSource TemplatedParent}}" />
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="BorderThickness"
                                                                           Storyboard.TargetName="PartBorder">
                                                <DiscreteObjectKeyFrame KeyTime="0" 
                                                                        Value="0" />
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="Disabled">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Opacity"
                                                                           Storyboard.TargetName="PartGrid">
                                                <DiscreteObjectKeyFrame KeyTime="0" 
                                                                        Value="0.6" />
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                </VisualStateGroup>
                                <VisualStateGroup x:Name="FocusStates">
                                    <VisualState x:Name="Focused" />
                                    <VisualState x:Name="Unfocused" />
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>

                            <Border Name="PartBorder" Background="Transparent">
                                <Grid>
                                    <Ellipse Name="PartEllipse" Fill="{TemplateBinding Foreground}" />
                                    <Image Source="{TemplateBinding ImageSource}" Stretch="Uniform" />
                                </Grid>
                            </Border>

                            <ContentPresenter Name="PartContentPresenter" Visibility="Collapsed" />

                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </UserControl.Resources>

    
    <flyout:FlyoutBase.AttachedFlyouts>
        
        <flyout:Flyout Key="TourMapFlyoutAttractionList"
                       IsFullScreen="True"
                       CloseOnNavigation="False"
                       Background="{StaticResource IziTravelLightGrayBrush}"
                       flyout:FlyoutBase.IsOpen="{Binding FlyoutAttractionListViewModel.IsOpen, Mode=TwoWay}">
            <i:Interaction.Behaviors>
                <flyout:FlyoutBehavior ClosedCommand="{Binding FlyoutAttractionListViewModel.ClosedCommand}" />
            </i:Interaction.Behaviors>
            <flyout:Flyout.Content>
                <Grid Margin="24,24,0,0" DataContext="{Binding FlyoutAttractionListViewModel}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    
                    <TextBlock Grid.Row="0"
                               Text="{Binding LocalizedResources.LabelAttractions, Source={StaticResource LocalizedStrings}, Converter={StaticResource StringToUpperConverter}}" 
                               Style="{StaticResource FlyoutTitleStyle}" />

                    
                    <ListView Grid.Row="1" ItemsSource="{Binding Items}">
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                <Setter Property="VerticalContentAlignment" Value="Stretch"/>
                            </Style>
                        </ListView.ItemContainerStyle>
                        <i:Interaction.Behaviors>
                            <behaviors:ListViewSelectBehavior Command="{Binding ItemSelectedCommand}" />
                        </i:Interaction.Behaviors>
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <modulecontrols:ListItemRowControl 
                                                                   Title="{Binding Title}" 
                                                                   ImageSource="{Binding ImageUrl}"
                                                                   Number="{Binding Order}"
                                                                   ShowNumber="True"
                                                                   Margin="0,0,24,12"
                                                                   d:DataContext="{d:DesignInstance viewmodels:TourMapItemViewModel}">

                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="Tap">
                                            <i:EventTrigger.Actions>
                                                <i:InvokeCommandAction Command="{Binding NavigateCommand}" />
                                            </i:EventTrigger.Actions>
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>

                                </modulecontrols:ListItemRowControl>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Grid>
            </flyout:Flyout.Content>
        </flyout:Flyout>

        
        <flyout:Flyout Key="TourMapFlyoutStartPrompt" 
                       Background="{StaticResource IziTravelLightBrush}"
                       flyout:FlyoutBase.IsOpen="{Binding FlyoutStartViewModel.IsOpen, Mode=TwoWay}">
            <i:Interaction.Behaviors>
                <flyout:FlyoutBehavior ClosedCommand="{Binding FlyoutStartViewModel.ClosedCommand}" />
            </i:Interaction.Behaviors>
            <flyout:Flyout.Content>
                <Grid Margin="24,24,0,0" DataContext="{Binding FlyoutStartViewModel}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    
                    <TextBlock Grid.Row="0"
                               Text="{Binding LocalizedResources.LabelStartTour, Source={StaticResource LocalizedStrings}, Converter={StaticResource StringToUpperConverter}}" 
                               Style="{StaticResource FlyoutTitleStyle}" />

                    
                    <TextBlock Grid.Row="1"
                               Text="{Binding LocalizedResources.LabelStartTourDescription, Source={StaticResource LocalizedStrings}}"
                               Style="{StaticResource FlyoutDescriptionStyle}" />

                    
                    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,24,0,0">
                        <Button Content="{Binding LocalizedResources.CommandCancel, Source={StaticResource LocalizedStrings}}"
                                Style="{StaticResource FlyoutButtonStyle}"
                                Command="{Binding CancelCommand}" />
                        <Button Content="{Binding LocalizedResources.CommandStart, Source={StaticResource LocalizedStrings}}"
                                Style="{StaticResource FlyoutButtonStyle}"
                                Command="{Binding StartCommand}" />
                    </StackPanel>
                </Grid>
            </flyout:Flyout.Content>
        </flyout:Flyout>

        
        <flyout:Flyout Key="TourMapFlyoutRoute"
                       IsFullScreen="True"
                       CloseOnNavigation="False"
                       Background="{StaticResource IziTravelLightGrayBrush}"
                       flyout:FlyoutBase.IsOpen="{Binding FlyoutRouteViewModel.IsOpen, Mode=TwoWay}">
            <i:Interaction.Behaviors>
                <flyout:FlyoutBehavior ClosedCommand="{Binding FlyoutRouteViewModel.ClosedCommand}" />
            </i:Interaction.Behaviors>
            <flyout:Flyout.Content>
                <Grid Margin="24,24,0,0" DataContext="{Binding FlyoutRouteViewModel}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    
                    <TextBlock Grid.Row="0"
                               Text="{Binding LocalizedResources.LabelRoute, Source={StaticResource LocalizedStrings}, Converter={StaticResource StringToUpperConverter}}" 
                               Style="{StaticResource FlyoutTitleStyle}" />

                    
                    <ListView Grid.Row="1" ItemsSource="{Binding Items}">
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                <Setter Property="VerticalContentAlignment" Value="Stretch"/>
                            </Style>
                        </ListView.ItemContainerStyle>
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <modulecontrols:ListItemRowControl 
                                                                   Title="{Binding Title}" 
                                                                   ImageSource="{Binding ImageUrl}"
                                                                   Number="{Binding Order}"
                                                                   ShowNumber="True"
                                                                   Margin="0,0,24,12"
                                                                   d:DataContext="{d:DesignInstance viewmodels:TourMapItemViewModel}">

                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="Tap">
                                            <i:EventTrigger.Actions>
                                                <i:InvokeCommandAction Command="{Binding NavigateCommand}" />
                                            </i:EventTrigger.Actions>
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>

                                </modulecontrols:ListItemRowControl>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>

                    
                    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,24,0,0">
                        <Button Content="{Binding LocalizedResources.CommandCancel, Source={StaticResource LocalizedStrings}}"
                                Style="{StaticResource FlyoutButtonStyle}"
                                Command="{Binding CancelCommand}" />
                        <Button Content="{Binding LocalizedResources.CommandStart, Source={StaticResource LocalizedStrings}}"
                                Style="{StaticResource FlyoutButtonStyle}"
                                Command="{Binding StartCommand}" />
                    </StackPanel>
                </Grid>
            </flyout:Flyout.Content>
        </flyout:Flyout>
    </flyout:FlyoutBase.AttachedFlyouts>

    
    <Grid x:Name="LayoutRoot">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Map View -->
        <maps:MapItemsControl x:Name="Map"
                      Grid.Row="0"
                      ItemsSource="{Binding MapItems}"
                      ZoomLevel="{Binding ZoomLevel, Mode=TwoWay}"
                      Center="{Binding Center, Mode=TwoWay}"
                      MapStyle="Road"
                      MapServiceToken="YOUR_BING_MAPS_API_KEY"
                      ItemClick="OnMapItemClick">
            <maps:MapItemsControl.ItemTemplate>
                <DataTemplate>
                    <maps:MapItemControl Location="{Binding Location}"
                                       Anchor="{Binding Anchor}"
                                       IsSelected="{Binding IsSelected, Mode=TwoWay}"
                                       IsHitTestVisible="{Binding IsHitTestVisible}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <!-- Play Content -->
                            <Grid Grid.Row="0"
                                  Margin="{Binding IsSelected, Converter={StaticResource MapPinIsSelectedToPlayContentMarginConverter}}"
                                  Visibility="{Binding IsPlayContentVisible, Converter={StaticResource BoolToVisibilityConverter}}">
                                <Ellipse Width="40" Height="40"
                                         Fill="{Binding State, Converter={StaticResource TourMapItemStateToBackgroundConverter}}"
                                         Stroke="{Binding State, Converter={StaticResource TourMapItemStateToBorderConverter}}"
                                         StrokeThickness="2" />
                                <Image Source="/Assets/Icons/appbar.control.play.png"
                                       Width="20" Height="20"
                                       Margin="2,0,0,0" />
                            </Grid>

                            <!-- Text Content -->
                            <Grid Grid.Row="1"
                                  Margin="{Binding IsSelected, Converter={StaticResource MapPinIsSelectedToTextContentMarginConverter}}"
                                  Visibility="{Binding IsTextContentVisible, Converter={StaticResource BoolToVisibilityConverter}}">
                                <Border Background="{Binding State, Converter={StaticResource TourMapItemStateToBackgroundConverter}}"
                                        BorderBrush="{Binding State, Converter={StaticResource TourMapItemStateToBorderConverter}}"
                                        BorderThickness="2"
                                        CornerRadius="2">
                                    <TextBlock Text="{Binding Title}"
                                               Foreground="{Binding State, Converter={StaticResource TourMapItemStateToForegroundConverter}}"
                                               Margin="5,2,5,2"
                                               FontSize="{StaticResource IziTravelFontSizeSmall}"
                                               FontFamily="{StaticResource IziTravelFontFamilySegoeSemibold}" />
                                </Border>
                            </Grid>
                        </Grid>
                    </maps:MapItemControl>
                </DataTemplate>
            </maps:MapItemsControl.ItemTemplate>
        </maps:MapItemsControl>

        <!-- Map Controls -->
        <StackPanel Grid.Row="0" 
                    VerticalAlignment="Top" 
                    HorizontalAlignment="Right"
                    Margin="0,12,12,0">
            <corecontrols:ActionRoundButton ImageSource="/Assets/Icons/appbar.map.route.png"
                                        Command="{Binding OpenRouteCommand}"
                                        Style="{StaticResource MapButton}"
                                        Margin="0,12,0,0" />
            <corecontrols:ActionRoundButton ImageSource="/Assets/Icons/appbar.map.layers.png"
                                        Command="{Binding OpenLayersCommand}"
                                        Style="{StaticResource MapButton}"
                                        Margin="0,12,0,0" />
        </StackPanel>

        
        <audio:AudioPlayerView Grid.Row="1" DataContext="{Binding AudioPlayerViewModel}" />
    </Grid>
</UserControl>