<UserControl x:Class="Izi.Travel.Mtg.Views.Common.List.SponsorListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             
             xmlns:list="clr-namespace:Izi.Travel.Mtg.ViewModels.Common.List"
             xmlns:commoncontrols="clr-namespace:Izi.Travel.Common.Controls"
             xmlns:i="using:Microsoft.Xaml.Interactivity"
             xmlns:behaviors="clr-namespace:Izi.Travel.Core.Components.Behaviors"
             
             mc:Ignorable="d">
   
    
    <UserControl.Resources>
        <DataTemplate x:Key="SponsorListItemTemplate">
            <Grid Height="90" 
                  Margin="0,0,24,24" 
                  Background="Transparent"
                   
                  d:DataContext="{d:DesignInstance list:ReferenceListItemViewModel}">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                
                <Grid Grid.Column="0" 
                      Width="90" 
                      Height="90"
                      Margin="0,0,20,0"
                      Background="{StaticResource IziTravelLightBrush}">
                    <Image Source="{Binding ImageUrl}"
                           Stretch="UniformToFill"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Visibility="{Binding ImageUrl, Converter={StaticResource NullToVisibilityConverter}}">
                    </Image>
                    <Path Data="M0,0 L1,0 0.5,1 Z" 
                          Fill="{StaticResource IziTravelDarkGrayBrush}"
                          Stretch="Uniform" 
                          HorizontalAlignment="Center"
                          VerticalAlignment="Center"
                          Width="24"
                          Height="24"
                          Visibility="{Binding ImageUrl, Converter={StaticResource NullToInverseVisibilityConverter}}">
                    </Path>
                </Grid>

                
                <TextBlock Grid.Column="1"
                           Text="{Binding Name}"
                           Foreground="{StaticResource IziTravelDarkBrush}"
                           FontSize="{StaticResource IziTravelFontSizeNormal}"
                           
                           MaxHeight="62"
                           TextWrapping="Wrap"
                           TextTrimming="WordEllipsis"
                           VerticalAlignment="Center" />
            </Grid>
        </DataTemplate>
    </UserControl.Resources>
    
    
    <Grid Margin="24,0,24,0">
        
        <ListView Margin="0,0,-24,0"
                  ItemsSource="{Binding Items}"
                  Visibility="{Binding IsListEmpty, Converter={StaticResource BoolToInvisibilityConverter}}"
                  ItemTemplate="{StaticResource SponsorListItemTemplate}"
                  IsItemClickEnabled="True"
                  ItemClick="ListView_ItemClick">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="VerticalContentAlignment" Value="Stretch"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.HeaderTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding LocalizedResources.HintSponsors, Source={StaticResource LocalizedStrings}}"
                               Margin="0,5,24,40"
                               TextWrapping="Wrap"
                               FontSize="{StaticResource IziTravelFontSizeNormal}"
                               
                               Foreground="{StaticResource IziTravelDarkGrayBrush}" />
                </DataTemplate>
            </ListView.HeaderTemplate>
            <ListView.FooterTemplate>
                <DataTemplate>
                    <Border Height="72" />
                </DataTemplate>
            </ListView.FooterTemplate>
        </ListView>
    </Grid>
</UserControl>