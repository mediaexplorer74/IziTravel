<UserControl x:Class="Izi.Travel.Mtg.Views.Common.List.ReviewListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:i="using:Microsoft.Xaml.Interactivity"
             xmlns:behaviors="clr-namespace:Izi.Travel.Core.Components.Behaviors"
             xmlns:controls="using:Izi.Travel.Controls"
             xmlns:rating="using:Izi.Travel.Controls"
             xmlns:placeholders="using:Izi.Travel.Controls.Placeholders"
             xmlns:local="using:Izi.Travel.Mtg.Converters"
             mc:Ignorable="d"
             d:DesignHeight="800"
             d:DesignWidth="400">

    <UserControl.Resources>
        <Storyboard x:Name="TiltStoryboard">
            <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.RenderTransform).(CompositeTransform.ScaleX)">
                <EasingDoubleKeyFrame KeyTime="0" Value="1"/>
                <EasingDoubleKeyFrame KeyTime="0:0:0.2" Value="0.95"/>
            </DoubleAnimationUsingKeyFrames>
            <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.RenderTransform).(CompositeTransform.ScaleY)">
                <EasingDoubleKeyFrame KeyTime="0" Value="1"/>
                <EasingDoubleKeyFrame KeyTime="0:0:0.2" Value="0.95"/>
            </DoubleAnimationUsingKeyFrames>
        </Storyboard>

        <!-- Simplified Rating Display using TextBlock with star characters -->
        <Style x:Key="AverageRatingStyle" TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Left" />
            <Setter Property="FontFamily" Value="Segoe UI Symbol" />
            <Setter Property="FontSize" Value="24" />
            <Setter Property="Foreground" Value="{StaticResource IziTravelVioletBrush}" />
        </Style>

        <Style x:Key="ReviewRatingStyle" TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Left" />
            <Setter Property="FontFamily" Value="Segoe UI Symbol" />
            <Setter Property="FontSize" Value="16" />
            <Setter Property="Foreground" Value="{StaticResource IziTravelDarkBrush}" />
            <Setter Property="Margin" Value="-2,0,0,0" />
        </Style>
        
        <local:RatingToStarsConverter x:Key="RatingToStarsConverter" />

        <Style x:Key="RateButtonStyle" TargetType="Button">
            <!-- Tilt effect removed for UWP compatibility -->
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid Background="Transparent">
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal"/>
                                    <VisualState x:Name="PointerOver">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Foreground" Storyboard.TargetName="TextBlock">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="White"/>
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="Pressed">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="(Shape.Stroke).(SolidColorBrush.Color)" Storyboard.TargetName="Ellipse">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource IziTravelLightBrush}"/>
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Foreground" Storyboard.TargetName="TextBlock">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="White"/>
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="Disabled">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="(Shape.Stroke).(SolidColorBrush.Color)" Storyboard.TargetName="Ellipse">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource IziTravelGrayBrush}"/>
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Foreground" Storyboard.TargetName="TextBlock">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource IziTravelGrayBrush}"/>
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Foreground" Storyboard.TargetName="LabelTextBlock">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource IziTravelGrayBrush}"/>
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                            <Grid Margin="{TemplateBinding Padding}" 
                                  Height="48" 
                                  VerticalAlignment="Top">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Ellipse Grid.Column="0"
                                         x:Name="Ellipse"
                                         Height="48"
                                         Width="48"
                                         Stroke="{StaticResource IziTravelBlueBrush}"
                                         StrokeThickness="3" />
                                <TextBlock Grid.Column="0"
                                           x:Name="TextBlock"
                                           Margin="9,-3,0,0"
                                           Text="★"
                                           FontSize="36"
                                           Foreground="{StaticResource IziTravelBlueBrush}" />
                                <TextBlock Grid.Column="1"
                                           x:Name="LabelTextBlock"
                                           Margin="23,3,0,0"
                                           Foreground="{StaticResource IziTravelBlueBrush}" 
                                           Text="{Binding LocalizedResources.CommandRateAndReview, Source={StaticResource LocalizedStrings}}" 
                                           FontSize="26" />
                            </Grid>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </UserControl.Resources>

    
    <Grid Margin="24,0,24,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <!-- Header Content -->
        <StackPanel Grid.Row="0" Margin="0,0,25,40">
            <StackPanel Height="136"
                        Orientation="Horizontal" 
                        Margin="0,0,0,1"
                        Visibility="{Binding IsEmpty, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=Inverted}">
                <Image Source="/Assets/Images/Reviews/empty_review.png" 
                       Width="136" 
                       Height="136"
                       Stretch="Uniform"
                       VerticalAlignment="Top"/>
                <StackPanel Margin="15,0,0,0">
                    <TextBlock Text="{Binding LocalizedResources.ReviewsEmptyTitle, Source={StaticResource LocalizedStrings}}" 
                               FontSize="24"
                               Foreground="{StaticResource IziTravelDarkBrush}" 
                               TextWrapping="Wrap"/>
                    <TextBlock Text="{Binding LocalizedResources.ReviewsEmptyMessage, Source={StaticResource LocalizedStrings}}" 
                               FontSize="16" 
                               
                               Foreground="{StaticResource IziTravelGrayBrush}" 
                               TextWrapping="Wrap" 
                               Margin="0,5,0,0"/>
                </StackPanel>
            </StackPanel>
            <Button x:Name="RateButton"
                    Style="{StaticResource RateButtonStyle}" 
                    Command="{Binding NavigateToRateCommand}" 
                    Visibility="{Binding CanRate, Converter={StaticResource BoolToVisibilityConverter}}" 
                    IsEnabled="{Binding CanRate}"
                    Margin="0,20,0,0"/>
            <TextBlock Text="{Binding LocalizedResources.ReviewsTitle, Source={StaticResource LocalizedStrings}}" 
                       FontSize="24"
                       Foreground="{StaticResource IziTravelDarkBrush}" 
                       Margin="0,40,0,20"/>
        </StackPanel>
        
        <!-- ListView for items -->
        <ListView Grid.Row="1"
                  Margin="0,0,-25,0"
                  ItemsSource="{Binding Items}"
                  Style="{StaticResource ListViewStyle}"
                  ItemContainerStyle="{StaticResource ReviewItemStyle}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,0,0,20">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <!-- Author and Date -->
                        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="12,12,12,0">
                            <TextBlock Text="{Binding AuthorName}" 
                                       FontSize="16"
                                       FontWeight="SemiBold"
                                       Foreground="{StaticResource IziTravelDarkBrush}"/>
                            <TextBlock Text="{Binding Created, Converter={StaticResource DateTimeToFormattedStringConverter}, ConverterParameter=' - {0:d}'}" 
                                       FontSize="14"
                                       Foreground="{StaticResource IziTravelGrayBrush}"
                                       Margin="10,0,0,0"/>
                        </StackPanel>
                        
                        <!-- Rating -->
                        <TextBlock Grid.Row="1"
                                   Margin="12,8,12,8"
                                   Text="{Binding Rating, Converter={StaticResource RatingToStarsConverter}}" 
                                   FontSize="24"
                                   Foreground="{StaticResource IziTravelYellowBrush}"/>
                        
                        <!-- Review Text -->
                        <TextBlock Grid.Row="2"
                                   Text="{Binding Text}"
                                   Margin="12,0,12,12"
                                   FontSize="14"
                                   Foreground="{StaticResource IziTravelDarkBrush}"
                                   TextWrapping="Wrap"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
            
            <!-- Empty State -->
            <ListView.Header>
                <StackPanel>
                    <TextBlock Text="{Binding IsListEmptyPlaceholder}"
                               Visibility="{Binding IsListEmpty, Converter={StaticResource BoolToVisibilityConverter}}"
                               Foreground="{StaticResource IziTravelGrayBrush}"
                               FontSize="{StaticResource IziTravelFontSizeNormal}"
                               Margin="0,20,0,0"/>
                </StackPanel>
            </ListView.Header>
            
            <!-- Loading Indicator -->
            <ListView.Footer>
                <Grid Height="100" 
                      Visibility="{Binding IsDataLoading, Converter={StaticResource BoolToVisibilityConverter}}">
                    <TextBlock Text="{Binding LocalizedResources.ActionLoading, Source={StaticResource LocalizedStrings}}" 
                               VerticalAlignment="Center"
                               HorizontalAlignment="Center"
                               TextWrapping="Wrap"
                               TextAlignment="Center"
                               
                               Foreground="{StaticResource IziTravelDarkGrayBrush}"/>
                </Grid>
            </ListView.Footer>

        </ListView>
    </Grid>
</UserControl>