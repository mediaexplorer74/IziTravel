<UserControl x:Class="Izi.Travel.Mtg.Views.Common.Numpad.NumpadFlyoutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:flyout="using:Izi.Travel.Core.Controls.Flyout"
             xmlns:i="using:Microsoft.Xaml.Interactivity"
             xmlns:components="using:Izi.Travel.Common.Components"
             xmlns:controls="using:Izi.Travel.Core.Controls"
             mc:Ignorable="d">

    
    <UserControl.Resources>
        <Flyout x:Name="NumpadFlyout"
                Placement="Full"
                Opening="NumpadFlyout_Opening"
                Closed="NumpadFlyout_Closed">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="190" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!-- Content will be moved here programmatically -->
                <ContentControl x:Name="NumpadContent" Grid.RowSpan="4" />
            </Grid>
        </Flyout>
        <Style x:Name="NumpadButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="80" />
            <Setter Property="Margin" Value="5,5,0,0" />
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource IziTravelLightBrush}"/>
     
            <Setter Property="FontSize" Value="42"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid Background="Transparent">
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal"/>
                                    <VisualState x:Name="MouseOver"/>
                                    <VisualState x:Name="Pressed">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Foreground" Storyboard.TargetName="ContentContainer">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource PhoneButtonBasePressedForegroundBrush}"/>
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Background" Storyboard.TargetName="ButtonBackground">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource IziTravelBlueBrush}"/>
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="Disabled" />
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                            <Border x:Name="ButtonBackground"
                                    Background="#33FFFFFF">
                                <ContentControl x:Name="ContentContainer"
                                                ContentTemplate="{TemplateBinding ContentTemplate}"
                                                Content="{TemplateBinding Content}"
                                                Margin="{TemplateBinding Padding}"
                                                Foreground="{TemplateBinding Foreground}"
                                                HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"/>
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    
    <!-- Progress overlay for busy state -->
    <Grid x:Name="ProgressOverlay" 
          Background="#CC000000" 
          Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibilityConverter}}"
          Canvas.ZIndex="1000">
        <ProgressRing IsActive="True"
                     Width="40"
                     Height="40"
                     Foreground="{StaticResource IziTravelLightBrush}" />
    </Grid>
    
    <!-- Old Flyout implementation removed - using standard UWP Flyout instead -->

</UserControl>