<Page x:Class="Izi.Travel.Mtg.Views.Common.RatePartView"
                            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                            xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                            Style="{StaticResource IziTravelPageStyle}"
                            mc:Ignorable="d">

    
    <Page.Resources>
        <!-- Simplified style for the rating slider -->
        <Style x:Key="RatingStyle" TargetType="Slider">
            <Setter Property="Width" Value="360" />
            <Setter Property="Height" Value="70" />
            <Setter Property="Background" Value="Transparent" />
        </Style>
    </Page.Resources>

    <!-- Replaced ProgressOverlay with ProgressRing for MVP -->
    <Grid>
        <ProgressRing IsActive="{Binding IsBusy}" HorizontalAlignment="Center" VerticalAlignment="Center" Width="50" Height="50"/>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="72" />
            </Grid.RowDefinitions>

            <StackPanel>
                <!-- Replaced PageTitleControl with TextBlock for MVP -->
                <TextBlock Text="{Binding LocalizedResources.RateTitle, Source={StaticResource LocalizedStrings}}" 
                           Style="{StaticResource HeaderTextBlockStyle}" 
                           Margin="0,0,0,20"/>

                <Grid Margin="24,0,24,0">

                    <TextBlock Foreground="{StaticResource IziTravelDarkGrayBrush}"
                               Text="{Binding LocalizedResources.RateLabelRating, Source={StaticResource LocalizedStrings}}" />

                    <TextBlock Margin="0,4,0,0" 
                               FontSize="{StaticResource IziTravelFontSizeSmall}"
                               Foreground="{StaticResource IziTravelDarkGrayBrush}"
                               Text="{Binding Hint}" 
                               HorizontalAlignment="Right" />

                </Grid>

                <Border Margin="24,10,24,20" 
                    Height="115"
                    Background="{StaticResource IziTravelLightBrush}">
                    <!-- Replaced custom RatingControl with basic Slider for MVP -->
                    <Slider Value="{Binding Rating, Mode=TwoWay}"
                            Minimum="0"
                            Maximum="5"
                            StepFrequency="1"
                            TickFrequency="1"
                            TickPlacement="Outside"
                            Width="360"
                            Margin="0,43,0,0"
                            HorizontalAlignment="Center"/>
                </Border>

                <TextBlock Margin="24,24,0,-6" 
                           Foreground="{StaticResource IziTravelDarkGrayBrush}"
                           Text="{Binding LocalizedResources.RateLabelName, Source={StaticResource LocalizedStrings}}" />

                <TextBox Margin="12,0,12,0" 
                         MaxLength="128"
                         Text="{Binding ReviewerName, Mode=TwoWay}">
                    <TextBox.InputScope>
                        <InputScope>
                            <InputScope.Names>
                                <InputScopeName NameValue="Text"/>
                            </InputScope.Names>
                        </InputScope>
                    </TextBox.InputScope>
                </TextBox>

                <TextBlock Margin="24,24,0,-6"
                           Foreground="{StaticResource IziTravelDarkGrayBrush}"
                           Text="{Binding LocalizedResources.RateLabelReview, Source={StaticResource LocalizedStrings}}" />

                <TextBox Margin="12,0,12,0"
                         MaxLength="500"
                         MaxHeight="200"
                         AcceptsReturn="True"
                         TextWrapping="Wrap"
                         Text="{Binding Text, Mode=TwoWay}" />

            </StackPanel>

            <Grid Grid.Row="1" 
                  Background="{StaticResource IziTravelDarkBrush}">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" 
                        Command="{Binding SubmitCommand}"
                        Foreground="{StaticResource IziTravelLightBrush}" 
                        BorderBrush="{StaticResource IziTravelLightBrush}"
                        Margin="12,0,-6,0"
                        Content="{Binding LocalizedResources.CommandSubmit, Source={StaticResource LocalizedStrings}, Converter={StaticResource StringToLowerConverter}}" />

                <Button Grid.Column="1"
                        Command="{Binding CancelCommand}"
                        Foreground="{StaticResource IziTravelLightBrush}" 
                        BorderBrush="{StaticResource IziTravelLightBrush}"
                        Margin="-6,0,12,0" 
                        Content="{Binding LocalizedResources.CommandCancel, Source={StaticResource LocalizedStrings}, Converter={StaticResource StringToLowerConverter}}" />

            </Grid>

        </Grid>
    </Grid>

</Page>