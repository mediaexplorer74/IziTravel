<UserControl x:Class="Izi.Travel.Mtg.Views.Common.QrCode.QrCodeScannerFlyoutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:flyout="using:Windows.UI.Xaml.Controls.Primitives"
             mc:Ignorable="d">

    
  
    
    <!-- Replaced Flyout with ContentControl for MVP to fix XAML compilation -->
    <Grid>
        <Grid Margin="25,30,25,0" Width="400" Background="{StaticResource IziTravelLightBrush}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0"
                       Text="QR CODE SCANNER" 
                       Style="{StaticResource FlyoutTitleStyle}" />

            <!-- Placeholder for barcode scanner -->
            <Border Grid.Row="1"
                    Margin="0,10,0,0"
                    Width="300"
                    Height="300"
                    Background="LightGray"
                    BorderBrush="DarkGray"
                    BorderThickness="1"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">
                <TextBlock Text="QR Code Scanner Placeholder"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           TextWrapping="Wrap"
                           TextAlignment="Center"/>
            </Border>

            <TextBlock Grid.Row="2" 
                       Margin="24,30,24,0"
                       TextAlignment="Center"
                       Foreground="{StaticResource IziTravelDarkBrush}"
                       FontSize="26"
                       
                       TextWrapping="Wrap"
                       Text="Point your camera at a QR code to scan it." />

            <Button Grid.Row="3"
                    Margin="0,30,0,30"
                    HorizontalAlignment="Center"
                    Content="Enter Code Manually"
                    Click="ManualEntryButton_Click"/>
        </Grid>
    </Grid>
</UserControl>